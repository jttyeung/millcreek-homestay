.about
  .container
    h2
      | About Us
    .row
    .photo style=background_image(image_path(data.about.about.family))
    .photo style=background_image(image_path(data.about.about.kitties))
    .row
    .content
      = markdown data.about.about.us